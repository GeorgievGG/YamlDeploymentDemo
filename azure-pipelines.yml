trigger:
    branches:
      include:
        - master
pool:
  vmImage: "ubuntu-latest"
  
steps:
- script: echo Hello, world!
  displayName: 'Run one-liner'
  
- script: |
	echo Add other tasks to build, test, and deploy ur project!
	echo See ...
  displayName: 'Run multi-liner'